import { FC } from 'react';
interface pageProps {}

const AdminDashboard: FC<pageProps> = ({}) => {
  return (
    <div className='flex flex-col items-center justify-center min-h-screen'>
      <div>Admin Dashboard</div>
    </div>
  );
};

export default AdminDashboard;
